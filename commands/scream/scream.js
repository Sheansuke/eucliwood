module.exports = {
  command: "",
  desciption: "Command to when someone writes in capital letters",
  run: (client, message, args) => {
    message.channel.send(`Por que me gritas :c`);
  },
};
